// config.ts

/**
 * URL base para o servidor Backend-for-Frontend (BFF).
 * Todas as chamadas de API do frontend devem ser direcionadas para este servidor.
 */
export const BFF_API_URL = 'http://localhost:3001';
